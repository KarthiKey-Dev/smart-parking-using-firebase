<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.iot.smartparking">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_car"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_car"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".paid2"></activity>
        <activity android:name=".free2" />
        <activity android:name=".free1" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".freeparking"
            android:label="@string/title_activity_freeparking" />
        <activity android:name=".parkingreportdetail" />
        <activity android:name=".booking" />
        <activity android:name=".login2" />
        <activity android:name=".paid1" />
        <activity android:name=".paidparking" />
        <activity android:name=".parkingslotlist" />
        <activity android:name=".fragmentMap" />
        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".MainActivity1stpage"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".fragmentList" />
        <activity android:name=".editprofile" />
        <activity android:name=".profile" />
        <activity android:name=".parking2" />
        <activity android:name=".parking1" />
        <activity
            android:name=".ParkingInfoActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".registeractivity" />
        <activity android:name=".viewbooking" />
        <activity android:name=".findparking" />
        <activity android:name=".splashactivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".UserProfileActivity" />
    </application>

</manifest>